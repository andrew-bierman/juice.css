/**
 * Demo page entry point
 * Single script that initializes all demo functionality
 * Bun's HTML bundler will bundle this and all its imports
 */

import "./theme-switcher";
import "./copy-button";
import "./dialog";
import "./syntax-highlight";
import "./raw-toggle";
